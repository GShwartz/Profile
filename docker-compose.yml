services:
  profile:
    build: .
    ports:
      - "8080:80"

  nginx:
    image: nginx:alpine
    volumes:
      - ./build:/usr/share/nginx/html
    ports:
      - "8080:80"
    expose:
      - "80"
    command: ["nginx", "-g", "daemon off;"]
